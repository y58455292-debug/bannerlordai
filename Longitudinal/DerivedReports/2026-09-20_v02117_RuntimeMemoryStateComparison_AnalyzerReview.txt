BannerlordAI v0.2.10.17 — Analyzer Review
Date: 2026-09-20
Final classification: PASS

Question
Can the natural patrol-defense shadow itself emit the exact prior/current five-scalar memory comparison for KNOWN_ENEMY, with independent math verification, interpretationApplied=false, and zero gameplay authority?

Independent findings
- Stage1 natural NEW on southern_pirates_47910 produced no memoryStateComparison object, as required.
- Stage1 snapshot persisted exactly through shadow -> acquisition -> episode.
- After guarded SaveAs/full restart and same-branch restore, exact learned Corsairs returned as KNOWN.
- Runtime patrol-defense shadow carried:
  memoryContext = exact recognition receipt
  memoryStateComparison.schema = BannerlordAI.PatrolDefenseThreatStateComparison.v1
  mode=observe
  prior
  current
  deltas
  currentToPriorRatios
  interpretationApplied=false
- A changed natural re-encounter occurred at memoryAgeHours=19.005398166715167.
- Prior:
  actorStrength=273.499969
  actorHealthy=213
  banditStrength=81.7
  banditHealthy=68
  actorToBanditStrengthRatio=3.34761286
- Current:
  actorStrength=273.499969
  actorHealthy=213
  banditStrength=81.9399948
  banditHealthy=66
  actorToBanditStrengthRatio=3.337808
- Independent validator recomputation matched runtime prior/current/deltas/ratios within numeric precision.
- Runtime comparison contains no semantic labels.
- No reacquisition, explicit retrieval, manual seed/recognize, Apply/rearm, behavior, intent, score or movement authority.
- All Stage1 and Stage2 checks true.
- Protected saves match, disposable saves cleaned, accepted binaries restored, lease released, Bannerlord closed.

Semantic conclusion
PASS. v0.2.10.17 moves exact factual then-vs-now memory comparison into the normal runtime Observe shadow with independently verified arithmetic and no policy semantics.

Architecture consequence
The first learned snapshot currently remains the comparison anchor forever because KNOWN re-encounters do not append new PatrolDefense observations. This is now the next architectural gap: memory must eventually evolve through immutable additional observations rather than overwrite the original episode or remain permanently frozen.

Next smallest gate
v0.2.10.18 — KNOWN re-encounter memory-update eligibility shadow.

Goal
On an exact KNOWN re-encounter with complete memoryStateComparison, emit a read-only memoryUpdateCandidate when at least one persisted/current scalar differs beyond numeric tolerance.

Rules
- Shadow only; no episode write yet.
- Exact identity and prior episode provenance required.
- Candidate should include:
  priorEpisodeId
  banditId
  prior/current snapshots
  changedFields
  proposed contextId
  proposed source
  proposed campaignHours
  appendOnly=true
  overwritePrior=false
  interpretationApplied=false
- If no factual change, eligible=false.
- If NEW_ENEMY, no re-encounter update candidate.
- Do not use labels such as danger/safe/retaliate/avoid.
- No behavior/intent/score/movement/Apply authority.
- Independent validator recomputes eligibility from raw memoryStateComparison.
- Natural nonappearance/unchanged window remains ENVIRONMENTAL_INCONCLUSIVE.

Why this order
Event-sourced immutable memory is already an adopted project architecture rule. Before allowing KNOWN encounters to mutate memory, prove the append-vs-overwrite decision surface and exact eligibility evidence in shadow. A later gate can append the second episode, then a following persistence gate can prove history contains both old and new observations and retrieval policy chooses the appropriate latest snapshot without losing lineage.

Productivity note
This gate is a minimal runner-only shadow extension; no ClanAI memory schema change is required yet.

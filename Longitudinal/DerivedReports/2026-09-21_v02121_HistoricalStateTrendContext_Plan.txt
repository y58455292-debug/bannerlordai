BannerlordAI v0.2.10.21 — Read-only historical factual-state trend context

Question
Can Manan derive deterministic numeric state change across the previous/latest persisted episodes for the exact same enemy identity, without semantic labels or gameplay authority?

Implementation boundary
- Extend the accepted v0.2.10.20 history-context reader; do not change the append writer or persistence schema.
- Parse the accepted factual threat snapshot fields already stored in each PatrolDefense episode detail:
  actorStrength, actorHealthy, banditStrength, banditHealthy, actorToBanditStrengthRatio.
- For histories with >=2 episodes, expose previousState, latestState, and latestMinusPrevious numeric deltas.
- Keep episode count/order/span/interval/provenance from v0.2.10.20.
- Do not emit labels such as safer, more dangerous, weaker, stronger, retaliate, avoid, etc.
- interpretationApplied=false; behaviorMutation=false; intentMutation=false; scoreMutation=false; nativeMovementCalls=0.

Acceptance
1. Use the deterministic history fixture, not a random natural changed-enemy gate.
2. Two same-identity episodes use real increasing CampaignTime and intentionally different factual state values.
3. After native SaveAs/full restart, exact history context reports episodeCount=2 and the same ordered IDs.
4. previousState and latestState match the two source episode details exactly.
5. latestMinusPrevious independently recomputes exact numeric deltas for all five fields within the accepted floating tolerance.
6. No semantic interpretation or gameplay authority.
7. Protected saves/binaries/lease/disposable fixture saves restore cleanly.

Do not repeat
Do not re-prove v0.2.10.19 append persistence or v0.2.10.20 count/order/span persistence unless contradictory evidence appears.

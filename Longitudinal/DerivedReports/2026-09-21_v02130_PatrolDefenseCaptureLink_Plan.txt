BannerlordAI v0.2.10.30 — Read-only exact capture-outcome companion

Question
Can current ClanAI join HeroPrisonerTaken capture facts to the same exact native lord-bandit MapEvent identity already accepted in v0.2.10.28, without guessing, memory writes, or gameplay authority?

Prior art reused
- Inspector v0.33 OutcomeIdentityBridge already implemented HeroPrisonerTaken with exact native reference resolution and observed two live captures.
- Its Resolve(a,b) rule rejects conflicting non-null native references and never falls back to names/time.
- v0.2.10.28 current ClanAI already owns a bounded exact-reference active/recent event index and live-proven start/end receipts.
Therefore reuse that mechanism; do not invent a parallel identity system.

Smallest implementation
- Extend v0.2.10.28 observer/index only.
- Add exact Resolve(a,b,out grade) to current bounded event index.
- Subscribe current observer to CampaignEvents.HeroPrisonerTaken.
- When capturer is a bandit, resolve capturer.MapEvent and prisoner.PartyBelongedTo.MapEvent by exact object reference.
- Emit BannerlordAI.PatrolDefenseCaptureObservation.v1:
  eventKey/linkGrade, prisonerHeroId/prisonerClanId, capturerBanditId/capturerHeroId,
  primaryActorPrisoner, capturerMatchesStartBandit, observedAfterEndCallback,
  memoryWriteApplied=false, interpretationApplied=false, behavior/intent/score mutation=false, nativeMovementCalls=0.
- Conflicting or untracked references remain explicit null/unlinked; never guess.

Acceptance
A. Current build compiles.
B. Isolated fixtures cover exact active/recent resolve, conflicting refs, untracked refs, duplicate/end retention, zero mutation.
C. One bounded live current-binary gate observes at least one capture receipt or is ENVIRONMENTAL_INCONCLUSIVE; generic v0.33 capture semantics are reused rather than re-proven from scratch.
D. No outcome/capture memory append; no Apply/engage changes.

BannerlordAI v0.2.10.31 — Read-only unified PatrolDefense outcome packet

Question
Can current ClanAI join start, capture (when present), and end facts for the same exact native MapEvent into one bounded session-local structured packet without persistence, guessing, semantic interpretation, or gameplay authority?

Prior art
- Accepted v0.2.10.28 proves exact native start/end object identity and explicit EXACT_UNIQUE/NONE/AMBIGUOUS history linking.
- Accepted v0.2.10.30 proves exact HeroPrisonerTaken capture linkage to the same event identity, including capture-before-end ordering.
- SkyrimNet exposes structured event schemas and recommends inspecting live event field structure before building triggers; persistent actor memory is a separate downstream concern.
- IntelEngine keeps task/meeting outcome facts structured and only then surfaces persisted outcome memory.
Therefore .31 should normalize already-proven facts into a session-local event packet, not write outcome memory.

Smallest implementation
- Extend the accepted v0.2.10.30 event index entry only with capture facts observed for the exact same native event reference.
- At capture: update only the tracked in-memory event entry with factual capture fields; continue emitting the existing capture receipt.
- At end: emit BannerlordAI.PatrolDefenseOutcomePacket.v1 containing:
  eventKey, actorId/partyId, banditId, actorSideAtStart,
  start/end campaign hours,
  linkResolution + exact history identity fields when unique,
  winningSide/hasWinner/battleState/actorWon,
  captureObserved boolean,
  prisonerHeroId/prisonerClanId/capturerBanditId,
  primaryActorPrisoner/capturerMatchesStartBandit/observedAfterEndCallback when capture exists.
- Session-local only. SyncData stays empty. No episode append.
- memoryWriteApplied=false; interpretationApplied=false; behaviorMutation=false; intentMutation=false; scoreMutation=false; nativeMovementCalls=0.
- No labels such as victory quality, danger, revenge, success, failure, threat, or confidence.

Acceptance
1. Current build compiles.
2. Isolated fixtures cover no-capture packet, capture-before-end packet, duplicate capture, wrong/untracked capture, exact event continuity, NONE/AMBIGUOUS/EXACT_UNIQUE memory-link fields, zero mutation.
3. One bounded live current-binary gate observes at least one packet matching its start/end event key. Capture-present packet is preferred but not required because capture occurrence is environmental.
4. No persistent outcome memory write; no Apply/engage changes.
5. Full integrity cleanup.

Blocked downstream gate
Do not append this packet into persistent PatrolDefense memory until a real MainHero outcome with EXACT_UNIQUE prior PatrolDefense history is observed. v0.2.10.29 remains parked environmental-inconclusive.

BannerlordAI v0.2.10.35 — Bounded session-local recent PatrolDefense outcome context

Question
Can current ClanAI retain and retrieve the latest accepted unified outcome packet as exact actor+branch+bandit session-local context without persistent memory mutation or gameplay authority?

Why now
- v0.2.10.31 provides the canonical unified outcome packet.
- v0.2.10.32-.34 complete future persistent-write eligibility, proposal, and idempotency scaffolding while the real MainHero+EXACT_UNIQUE writer barrier remains unmet.
- ActorBlackboard / GroveGames prior art supports bounded typed transient state distinct from long-term memory.
This lets outcome facts become useful same-session context without falsely claiming persistence.

Smallest implementation
- Add PatrolDefenseRecentOutcomeContext as a separate session-local component.
- Exact key = actorId + branchId + banditId.
- Store latest unified packet only after packet construction.
- Bound to 128 exact keys; evict oldest inserted/updated key deterministically.
- No SyncData, no save key, no DynastyBranchEpisodeMemory mutation.
- Exact retrieval only; result FOUND or NONE.
- Expose read-only bridge/TestRunner command:
  PATROL_DEFENSE_RECENT_OUTCOME_CONTEXT <actorId> <banditId> [context]
- Receipt schema BannerlordAI.PatrolDefenseRecentOutcomeContext.v1:
  FOUND/NONE, exact actor/branch/bandit, retrievalContext,
  eventKey and all stored factual outcome/history/capture fields when FOUND,
  persistentMemoryMutation=false; interpretation/gameplay authority zero.

Acceptance
A. Build current ClanAI + TestRunner.
B. Fixtures: exact key FOUND, wrong actor NONE, wrong branch NONE, wrong bandit NONE, latest replacement, 128-key deterministic eviction, null/invalid key rejection, no persistence surface.
C. Live: observe one natural lord-bandit unified packet, then exact explicit same-session retrieval using that packet actorId+banditId returns FOUND with identical eventKey and factual fields.
D. Wrong-bandit probe returns NONE.
E. EXIT_NOSAVE, protected saves/binaries/lease/disposable cleanup PASS.
F. This is transient context only; persistent outcome writer remains blocked on v0.2.10.29.

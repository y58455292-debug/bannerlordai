BannerlordAI v0.2.10.68 - Provider-specific controlled execution configuration

State
v0.2.10.67 closes the provider-neutral package/adapter boundary. The next capability requires a real provider selection plus explicit authorization for provider-network execution and credential resolution.

Required user decision
1. Select the first controlled provider/model.
2. Explicitly authorize bounded outbound provider API use for the BannerlordAI deliberation pipeline.
3. Provide the credential through a local environment-variable reference; do not place the secret value in project logs or durable evidence.

Existing research
DeepSeek V4.1 Flash is researched and marked CONTROLLED INTEGRATION RECOMMENDED, but its saved assessment recommends it primarily as an external high-volume analyst/compactor/anomaly scout and explicitly not as hot-loop action execution. That research is not itself authorization to use its API for live Bannerlord cognition.

Once authorized
- create provider-specific adapter/config with explicit endpoint/model and opaque credentialRef;
- add credential-presence preflight without logging the value;
- enforce existing timeoutMs/maxAttempts/maxResultBytes and latest authorization;
- make one bounded real provider call from a validated ProviderExecutionPackage;
- admit only strict TransportReceipt.v4 + ProviderResult.v5;
- preserve advisory-only/no-gameplay-authority scope for the first real-model proof;
- EXIT_NOSAVE and full integrity cleanup.

No real provider/network/credential action is authorized until the user explicitly grants it.

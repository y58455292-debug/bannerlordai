BANNERLORDAI — STRATEGIC COMPOSER SHADOW v0.20L REVIEW
Date: 2026-09-18 Pacific.

Purpose:
Prepare migration from multiple independent strategic score writers to one Strategic Decision Composer without changing behavior first.

Current strategic score-write owners preserved:
1. VisualWarDecisionLayer
2. weak-recovery rescore in ClanAIStrategicBehavior
3. SocialMemoryCausalLayer Apply path

v0.20L does NOT remove, reorder, or alter any existing write.
It adds a bounded read-only shadow around the existing pipeline.

Prior-art basis:
Utility-AI architectures centralize candidate scoring and named considerations under one selector/orchestrator.
Action-ownership doctrine from the project's external-agent research recommends one action -> one owner -> one write authority.
Shadow stages captured:
- vanilla input score list
- after VisualWar
- after weak-recovery stage
- after social-memory stage

Coverage:
- at most 300 Sturgia/Vlandia targeted decisions
- at most 50 global/non-targeted control decisions
- no unbounded capture
- no screenshots
- no persistent candidate-row dump

Checks:
- candidate list count and candidate identity remain stable across stages
- all scores remain finite
- visual/recovery sequential factors recompose numerically
- Observe-mode memory causes zero unexpected score mutation
- global non-targeted decisions do not change after the visual stage
- overlap count records candidates touched by both visual and recovery stages

Only mismatches are logged individually, capped at 12 records.
Performance design:
The shadow uses temporary in-memory arrays only for the bounded sample set.
It does not write per-candidate telemetry on success.
It stops sampling after the fixed budget.
This is intentionally temporary instrumentation to support the composer migration and should be removed after equivalence is established.

Behavior safety:
- StrategicCompositionShadow contains zero SetBehaviorScore calls.
- no movement/pathfinding/target writes
- no save writes
- no bandit changes
- pursuit suppression unchanged
- memory remains Observe
- existing score-write-site count remains exactly 3

Build:
v0.20L-review
FileVersion 0.20.12.0
InformationalVersion v0.20L-review-strategic-composer-shadow
0 errors; inherited System.ValueTuple warning only.
Structural audit PASS:
- shadow has no behavior writes
- total strategic score-write sites unchanged at 3
- reset and all stage hooks present
- bounded sampling present
- mismatch-only detailed logging present

DLL SHA256:
A9E22602963367DF5CCCF4191949AE2D0A86C4E386802AA37378F47B311975CD

PDB SHA256:
FE53BB1230AA755E683DBA87DD820528BA18B549E3F8B753DEB16105D0FB6512

Runtime gate:
One automated bounded replay.
Stop immediately on COMPOSER_SHADOW_MISMATCH.
Otherwise stop when sample budget is effectively filled or after the bounded time window.
Success requires:
- sampled decisions > 0
- visual and recovery changes both observed
- preferably some overlap candidates
- shape mismatches = 0
- numeric mismatches = 0
- memory unexpected mutations = 0
- non-finite = 0
- no performance/writer failures

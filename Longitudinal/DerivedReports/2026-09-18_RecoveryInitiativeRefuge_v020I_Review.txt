BANNERLORDAI — RECOVERY INITIATIVE REFUGE v0.20I OFFLINE REVIEW
Date: 2026-09-18 Pacific.

Parent finding:
v0.20H proved the exact threat identity becomes available at the native MobilePartyAIModel.GetBestInitiativeBehavior boundary, not reliably in the earlier strategic settlement scorer.
Morcon's last strategic risk record still showed GoToSettlement / no short-term target ~18.9 real seconds before battle.
Native initiative telemetry then showed FleeToPoint target=deserters_1716 ~14 seconds before the exact defender battle against deserters_1716.
Qaban also entered a weak defender battle while FleeToPoint targeted the exact southern_pirates_1542, with no matching strategic risk trigger.

v0.20I purpose:
Observe the exact native initiative moment only.
When TaleWorlds returns:
- independent AI lord
- weak by research threshold
- best initiative behavior = FleeToPoint
- best initiative target = bandit
the observer records exact threat and nearby friendly fortification context.
No behavior, score, target, direction, movement, pathfinding, or settlement state is mutated.
Observer context:
- exact native bandit threat party and initiative score
- readiness / food days / campaign hours
- native FleeToNearbySettlementRadius
- straight-line threat distance
- current strategic TargetSettlement and native path distance
- count of valid friendly fortifications in the native radius
- nearest valid friendly fortification by native MapDistanceModel path distance
- nearest refuge straight-line and path distance
- current short-term and default behavior

Valid refuge filter:
- Settlement.IsFortification
- same MapFaction by native object reference
- not under siege
- not under raid
- inside active native FleeToNearbySettlementRadius
- native MapDistanceModel returns a finite reachable distance

Performance guard:
Context is evaluated only for weak independent lords after the native result is FleeToPoint against a bandit.
Logs are rate-limited per actor-threat pair to once per 0.5 campaign hours.
The broad v0.20F-H strategic risk scanner was removed from the active scoring path.
Native behavior research:
AiBehavior includes FleeToGate.
Historical telemetry shows vanilla FleeToGate is actively used and can coexist with settlement targets / settlement entry.
Reflection confirms MobileParty.SetShortTermBehavior signature:
Void SetShortTermBehavior(AiBehavior newBehavior, IInteractablePoint mapEntity).
This is documented only for future design review; v0.20I does not call this method.

Safety audit:
- new observer contains no SetBehaviorScore
- no SetShortTermBehavior
- no SetTargetSettlement
- no SetMove / SetAiBehavior / SetInitiative
- observer is hooked after inner GetBestInitiativeBehavior returns
- existing weak EngageParty suppression remains unchanged
- old strategic recovery-risk mutation path is retired

Build:
0 errors; inherited System.ValueTuple warning only.
DLL SHA256: 6481A46B1AEB2E85E4BACFD1223295B16EBF9FDD5F3F6C2EC9FB2BCF213DE900
PDB SHA256: F8061C485E7129E56491F71214F21FF0340E22AAF7EF119CECBAD6089E11F319

Runtime question:
When an exact weak-lord bandit FleeToPoint initiative appears, how often does a valid friendly fortification exist within Bannerlord's own flee-settlement radius?
If one exists, record which refuge and distance; do not redirect yet.
If none exists in the dangerous same-bandit episodes, settlement-refuge rerouting is not the right mechanism.

BannerlordAI v0.2.10.15 — Prior/Current Threat-State Memory Shadow

Goal
Persist a tiny factual snapshot from the already-computed natural patrol-defense threat and prove that, on a later exact KNOWN re-encounter, the same shadow receipt contains both prior and current factual state so a deterministic comparison is possible without changing policy.

Fields
- actorStrength
- actorHealthy
- banditStrength
- banditHealthy
- actorToBanditStrengthRatio

Constraints
- Reuse values already computed by PatrolDefenseShadow.Evaluate; no new world scan.
- Existing RecordPatrolDefenseEpisode API remains intact for old seeds/backward compatibility.
- Add a state-aware writer path only for natural observer acquisitions.
- Persist fields in the existing episode Detail provenance; do not create a second memory store or schema family.
- Current threat facts remain in patrol-defense shadow as they already are.
- Later memoryContext.priorEpisode.detail must preserve prior facts exactly.
- Validator computes prior/current deltas as evidence; runtime policy remains unchanged.
- No aggression/avoidance/pursuit interpretation.
- No Apply/rearm, intent, score or movement authority.

Stage1 acceptance
- natural NEW
- shadow memoryContext exact as v0.2.10.14
- autonomous episode exactly once
- persisted episode detail contains five snapshot fields equal to the Stage1 shadow facts
- acquisition receipt carries same snapshot
- save/restart clean

Stage2 acceptance
- positive-time later natural KNOWN exact same bandit
- shadow memoryContext exact as v0.2.10.14
- priorEpisode exact Stage1 episode
- prior snapshot parses from detail and equals Stage1 facts
- current shadow contains current facts
- validator emits factual deltas/ratios; no policy conclusion
- no reacquisition/manual retrieval/seed/recognize/Apply/mutation
- full integrity clean

Performance
Five scalar values are copied from existing locals; no extra party enumeration, pathfinding query or global scan. Storage growth is bounded to a short provenance suffix per acquired episode.

BannerlordAI v0.2.10.19 — Eligible KNOWN Append-Only Memory Update Write

Question
Can an exact KNOWN re-encounter with memoryUpdateCandidate.eligible=true append exactly one new factual PatrolDefense episode, while eligible=false writes nothing, preserving the prior episode and retaining zero gameplay authority?

Implementation boundary
- Runner-only orchestration change; reuse the accepted ClanAI RecordPatrolDefenseEpisodeWithState bridge unchanged.
- Do not change the ClanAI memory schema.
- Build recognition -> memoryStateComparison -> memoryUpdateCandidate first.
- NEW_ENEMY keeps the existing initial acquisition path only.
- KNOWN + eligible=false: no writer call.
- KNOWN + eligible=true: call the existing state-bearing writer once for the current proposal, with a distinct update source such as PatrolDefenseObserver.v02119.known_enemy_changed_update and restoreReason=observer_known_enemy_changed_update.
- Preserve proposal-fingerprint dedup and append-only event storage. Never overwrite or mutate the prior episode.
- Emit a factual write receipt linking priorEpisodeId -> newEpisodeId; no semantic danger/safety labels.
- behaviorMutation=false, intentMutation=false, scoreMutation=false, nativeMovementCalls=0. No PatrolDefenseApplyGate/SetBehaviorScore/movement authority.

Acceptance
1. Stage 1 natural NEW creates exactly one initial state-bearing episode; NEW has no update candidate.
2. SaveAs + full restart restores same branch and exact original episode.
3. An unchanged exact KNOWN emits eligible=false/changedFields=[] and creates no episode.
4. A naturally changed exact KNOWN emits eligible=true with independently recomputed exact changedFields and appends exactly one new state-bearing episode with a new 32-char ID.
5. Immediate exact readback shows visibleCount=2, latest=new episode, source=v02119 update source, current factual snapshot exact, and original priorEpisodeId remains the candidate lineage.
6. Save after the update and full restart again. Exact retrieval still shows visibleCount=2 and latest=new episode, proving native persistence of the append.
7. No third/duplicate write for the same proposal fingerprint.
8. Zero gameplay authority; protected saves/binaries/lease/disposable saves all restore cleanly.

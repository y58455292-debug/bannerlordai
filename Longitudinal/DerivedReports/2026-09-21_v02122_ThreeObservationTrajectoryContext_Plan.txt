BannerlordAI v0.2.10.22 — Read-only three-observation factual trajectory context

Question
Can Manan expose a deterministic ordered three-observation factual trajectory for the exact same enemy identity, including each stored state, per-interval numeric deltas, and oldest-to-latest cumulative delta, without semantic interpretation or gameplay authority?

Implementation boundary
- Extend the accepted v0.2.10.21 read-only history-context reader only.
- Do not change episode writer, persistence schema, recognition, movement, scoring, or decision authority.
- For >=3 exact same-identity episodes expose:
  orderedStates: one factual state object per ordered episode.
  intervalDeltas: numeric latest-minus-previous for each adjacent pair.
  oldestToLatestDelta: exact numeric latest-oldest delta across all five persisted threat fields.
- Preserve accepted count/order/timing/provenance and v0.2.10.21 previous/latest/latestMinusPrevious fields.
- No qualitative labels (stronger/weaker/safer/more dangerous/trend up/down/etc.).
- interpretationApplied=false; behaviorMutation=false; intentMutation=false; scoreMutation=false; nativeMovementCalls=0.

Deterministic fixture
Use three validation-only same-identity observations at real increasing CampaignTime:
1) actorStrength=120, actorHealthy=100, banditStrength=60, banditHealthy=50, ratio=2.0
2) actorStrength=90, actorHealthy=80, banditStrength=55, banditHealthy=45, ratio=1.63636363
3) actorStrength=75, actorHealthy=70, banditStrength=50, banditHealthy=40, ratio=1.5

Expected adjacent deltas:
1->2 = [-30,-20,-5,-5,-0.36363637]
2->3 = [-15,-10,-5,-5,-0.13636363]
Expected oldest->latest = [-45,-30,-10,-10,-0.5]

Acceptance
1. Pre-save product receipt reports episodeCount=3 and exact ordered episode IDs.
2. orderedStates exactly match all three fixture source states.
3. intervalDeltas exactly match independent recomputation for both adjacent intervals within accepted numeric tolerance.
4. oldestToLatestDelta exactly matches independent recomputation.
5. Native SaveAs/full restart preserves count/order/states/deltas exactly.
6. No semantic interpretation or gameplay authority.
7. Protected saves/binaries/lease/disposable saves restore cleanly.

Do not repeat
Do not re-prove .19 append persistence, .20 temporal ordering, or .21 two-state delta math unless contradictory evidence appears.

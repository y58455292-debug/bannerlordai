BannerlordAI v0.2.10.33 — Read-only outcome-memory append proposal shadow

Question
Can current ClanAI deterministically construct the exact immutable PatrolDefense outcome episode payload that a future writer would append, but only from an eligible v0.2.10.32 candidate and without writing memory?

Reuse
- v0.2.10.31 unified packet is the source of truth for outcome facts.
- v0.2.10.32 eligibility is the sole gate.
- Existing PatrolDefense episode schema/provenance and append-only semantics from v0.2.10.19 remain authoritative.
- Event-sourcing architecture requires immutable append payloads and explicit provenance before write ownership is enabled.

Smallest implementation
- Add pure PatrolDefenseOutcomeMemoryProposalBuilder.
- Input: unified packet + evaluated eligibility candidate.
- If candidate is null/ineligible, return no proposal.
- If eligible, produce BannerlordAI.PatrolDefenseOutcomeMemoryProposal.v1 containing:
  actorId, branchId, banditId,
  settlementId, targetContextId,
  sourceEventKey, sourceLatestEpisodeId, priorHistoryEpisodeCount,
  knownByHours=endCampaignHours,
  source=PatrolDefenseOutcomeObserver.v02133.future_append_proposal,
  immutable factual detail fields copied from the packet:
    actorSideAtStart, start/end hours, winningSide, hasWinner,
    battleState, actorWon, actorAliveAtCallback, actorPrisonerAtCallback,
    captureObserved and capture facts when present.
- Proposal contains no new episode ID; the future writer owns ID creation.
- Emit proposalEligible=true and memoryWriteApplied=false.
- Ineligible runtime packet emits proposalEligible=false / proposal=null or no proposal receipt; never fabricate one.

Acceptance
A. Fixtures: eligible packet -> exact proposal; ineligible candidate -> no proposal; proposal copies exact context/provenance/outcome/capture facts; no episode ID generated.
B. Product build/static checks prove no outcome writer, no SyncData addition, no Apply path.
C. Bounded natural live negative gate reuses ordinary non-MainHero/NONE outcome and proves no proposal is produced while eligibility candidate is false.
D. Positive live proposal remains environmental and not required.
E. Full integrity cleanup.

Blocked writer
Do not add the actual append writer until a real MainHero + EXACT_UNIQUE live outcome exists. v0.2.10.29 remains parked.

BannerlordAI v0.2.10.51 FINAL VERIFY - ACCEPT

Decisive evidence
- Runner compiled with 0 errors; ClanAI reused unchanged.
- Claim-bound provider-result fixture suite passed 160/160.
- Natural request enqueued one job and exact claim deterministic_external_worker/attempt-1 transitioned it to CLAIMED.
- Exact SUCCESS provider result reported claimMatchReason=CLAIM_MATCHED before strict advisory admission.
- providerResultAccepted=true; nested advisory admitted=true / KEEP_BASELINE.
- SUCCESS completion emitted SUCCESS_ACKED and removed queue job 1 -> 0.
- Immediate identical result replay failed at claim lookup with claimMatchReason=DISPATCH_JOB_NOT_FOUND, rejectionReasons=[DISPATCH_JOB_NOT_FOUND].
- Replay completion was also DISPATCH_JOB_NOT_FOUND with queue 0 -> 0.
- Fixtures prove unclaimed, wrong provider, wrong attempt, missing job, transient, permanent, malformed, success and replay families.
- Provider invocation stream remained empty; no network/model/Apply authority.
- Every live check passed; protected saves matched, disposable cleaned, binaries restored, lease released.

Classification: ACCEPT.

Architecture meaning
A provider result can no longer consume a pending deliberation request merely by matching the request fingerprint. Envelope providerId + attemptId + fingerprint must first match the active CLAIMED dispatch job. Result provenance is now bound to exclusive worker ownership.

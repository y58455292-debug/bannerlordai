BannerlordAI v0.2.10.18 Analyzer Review

Classification
PASS

Independent findings
- NEW_ENEMY shadow contains no memoryUpdateCandidate.
- At least one exact KNOWN re-encounter had equal prior/current factual state and emitted eligible=false with changedFields=[].
- A later naturally changed exact KNOWN independently recomputes changedFields=['actorStrength', 'actorHealthy', 'actorToBanditStrengthRatio'] under the accepted 1e-5 relative tolerance and the runtime candidate matches exactly.
- Both candidates bind the exact original episode a42995c80421437c95b2e20befc9c356, settlement town_ES7, bandit southern_pirates_47910, exact context ID, and proposal campaign time.
- appendOnly=true, overwritePrior=false, interpretationApplied=false.
- behaviorMutation=false, intentMutation=false, scoreMutation=false, nativeMovementCalls=0.
- The candidate builder contains no memory writer or gameplay-authority call.
- Post-event exact readback remains visibleCount=1 with original episode a42995c80421437c95b2e20befc9c356; this milestone does not write a second episode.
- Protected saves match, disposable saves are cleaned, accepted binaries are restored, deployment lease is released.

Semantic result
ACCEPTED_MEMORY_UPDATE_ELIGIBILITY_SHADOW

Next smallest gate
v0.2.10.19 should reuse the existing append-capable patrol-defense writer only when an exact KNOWN memoryUpdateCandidate is eligible=true. eligible=false must write nothing. A changed KNOWN must append exactly one new state-bearing episode without overwriting the prior episode or gaining gameplay authority. Persist that update, restart again, and prove exact retrieval shows visibleCount=2 with the new episode as latest while the original prior episode remains preserved by append-only count/provenance.

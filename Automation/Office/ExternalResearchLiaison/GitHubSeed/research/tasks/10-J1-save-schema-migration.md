# J1 — Save Schema Evolution
Priority: P2

Research versioned persistence patterns for mod state. Define backward-compatible defaults, migrations, branch/timeline handling, corruption fallback, schema IDs, test matrix and byte-growth constraints.

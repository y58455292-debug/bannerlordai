# Phase 3 Home Responsibility — Deterministic Test Result

Date: 2026-09-25

## Scope

This checkpoint implements only the offline deterministic seam defined by the Phase 3 source audit. It does not deploy a new DLL and does not claim new campaign/runtime behavior.

## Implementation

Added `HomeResponsibilityPolicy`, a pure helper with no Bannerlord types. `HomeResponsibilityLayer` still receives Bannerlord's existing `PartyThinkParams` candidate list, reads the composer's current native-derived score, and retains the existing `WorldScopeContext.EligibleIndependentLordAtWar` gate. It now delegates only ownership/positive-score eligibility and factor/reason selection to the pure policy.

The existing winner-change expectation, pending native behavior/target verification, and `CompanionDutyMemory.RecordSuccessfulHomeDuty` call after a matched native commit remain in `HomeResponsibilityLayer`.

## Deterministic cases

The standalone policy test covers ten cases:

- actor not eligible at war -> no change;
- foreign settlement -> no change;
- non-positive candidate score -> no change;
- owned settlement under siege -> 1.60, with threat precedence over weak recovery;
- owned settlement under raid -> 1.60;
- weak lord going to an owned home settlement -> 1.35;
- defend owned home settlement -> 1.30;
- patrol around owned home settlement -> 1.25;
- ordinary go-to owned home settlement during war -> 1.15;
- unrelated native behavior -> no change.

## Validation

`dotnet run --project Tests/TerritorialResponsibility/TerritorialResponsibilityPolicyTests.csproj -c Release`

Result:
`PASS HomeResponsibility policy tests cases=10`

`python Tests/TerritorialResponsibility/test_runtime_wiring.py`

Result:
`PASS HomeResponsibility runtime wiring`
`native candidate list and post-vanilla commit verification preserved`

`dotnet build src/ClanAI/src/ClanAI/ClanAI.csproj -c Release`

Result: build succeeded with 0 errors. The one existing `System.ValueTuple` version-conflict warning remains unchanged.

## Boundary

No native candidates are generated by this seam. No direct party order, faction transfer, ownership mutation, or war action was added. No runtime deployment was performed. This is an offline implementation/test checkpoint only; any later runtime claim must use the established Inspector/TestRunner path and verify Bannerlord's actual selected behavior/target after rescoring.
